apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.name }}
spec:
  selector:
    app: {{ .Values.global.name }}
  ports:
  - port: {{ .Values.app.port }}
    targetPort: {{ .Values.app.port }}
